export default function BlogNew() {
  return (
    <section>
      <h1>Blog New</h1>
      {/* {isModal && (
        <Modal titleModal="Create New Blog" isModal={isModal} onClose={handleCloseModal1}>
          <FormBlog onSend={handleSend} isSelect />
        </Modal>
      )}

      <button onClick={handleOpenModalDel}>show dialog</button>

      {isModalDel && (
        <Modal titleModal="Delete Blog" isModal={isModalDel} onClose={handleCloseModal2} width="md">
          <ConfirmDelete _id="123" title="Are you sure" description=" You want to delete this blog " />
        </Modal>
      )} */}
    </section>
  );
}
